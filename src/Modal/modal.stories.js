import React from "react";
import { Dialog } from "../index";
import { Section } from "../index";

export default {
  title: "Dialog"
};

export const Theme = () => (
  <Section title="Dialogs - Theme">
  </Section>
);
