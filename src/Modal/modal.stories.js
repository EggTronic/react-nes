import React from "react";
import { Modal } from "../index";
import { Section } from "../index";

export default {
  title: "Modal"
};

export const Theme = () => (
  <Section title="Modal - Theme">
  </Section>
);
